# 🧮 计算器（calc）

四则运算计算器 demo，验证复杂前端 UI 场景。

## 功能
- 数字键 `0-9`、小数点
- 运算符 `+ − × ÷ %`
- `C` 清空 / `⌫` 退格 / `=` 求值
- 历史记录（最近 10 条，仅内存）

## 验证什么
1. iframe 内复杂键盘交互（按钮网格、状态机）
2. 表达式求值（含错误处理，例如除零、非法字符）
3. 无持久化场景（刷新后状态清空）

## 文件
- `app.json` —— 应用清单
- `app.py` —— 单文件 HTTP 服务（HTML/CSS/JS 内联）
